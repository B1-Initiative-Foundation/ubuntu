B1-Initiative-Foundation/sankofapedia@v4:  # Correctly formatted destination repo and branch
  - source: content  # Correctly specifying the relative path from the root of the source repo
    dest: content  # Destination path in the target repo
